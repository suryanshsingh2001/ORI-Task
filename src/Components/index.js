/**
 * This file exports the components used in the application.
 */

import SearchBar from "./SearchBar";
import Header from "./Header";
import Layout from "./Layout";

/**
 * Exported components:
 *
 * SearchBar - Component responsible for handling the search functionality.
 * Header - Component representing the header section of the application.
 * Layout - Component for displaying the layout and rendering the fetched photos.
 */
export {
    SearchBar,
    Header,
    Layout,
};
